# Assignment 4

## Changelog

* Refactored the entire project into a Single Page Application (SPA).
* Consolidated all separate HTML pages into a single `index.html`.
* Implemented client-side routing using JavaScript to switch views without page reloads.
* Replaced static HTML tables with dynamic content fetching:
  * Data is now retrieved exclusively via Fetch API from JSON files simulating a REST service.
* Updated file structure to support the SPA architecture.
